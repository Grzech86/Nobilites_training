package Nobilities.ex37;

import java.util.Collection;

public class Database {
    public Database(Collection<City> city) {
    }

    public boolean City (String town,String twoChar ){

        return (town.indexOf(town) == twoChar.indexOf(twoChar));
    }

}
